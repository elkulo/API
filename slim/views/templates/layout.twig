<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name='robots' content='noindex,nofollow' />
<title>{% if title is defined %}{{ title }}{% endif %}API &#124; el.kulo</title>
{% if description is defined %}
<meta name="description" content="{{ description }}">
{% endif %}
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="shortcut icon" type="image/png" href="/favicon.ico"/>
<link href="https://fonts.googleapis.com/css?family=Noto+Serif+JP|200,400,600&amp;display=swap" rel="stylesheet"/>
<style>
@keyframes AnimationGradient {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}
* {
  transition: background-color 300ms ease, color 300ms ease;
}
*:focus {
  background-color: rgba(221, 72, 20, 0.2);
  outline: none;
}
html,
body {
  color: rgba(33, 37, 41, 1);
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial,
    sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
  font-size: 16px;
  margin: 0;
  padding: 0;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
  width: 100%;
  height: 100%;
}

a {
  color: #fff;
  text-decoration: none;
}

a:hover,
a:focus {
  color: #ddd;
  text-decoration: underline;
}

pre > code {
  overflow: scroll;
  display: block;
  background: #f5f5f5;
  color: #2f6f9f;
  border: solid 1px #bbb;
  border-radius: 5px;
  padding: 0.5rem 0.75rem;
  font-size: 1.25rem;
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.2;
  white-space: nowrap;
}

.site {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  width: 100%;
  height: 100%;
  background: linear-gradient(230deg, #a24bcf, #4b79cf, #4bc5cf);
  background-size: 300% 300%;
  animation: AnimationGradient 30s ease infinite;
}

.site-header {
  margin: auto;
}

.site-footer {
  margin-top: auto;
}

.heroe {
  box-sizing: border-box;
  margin: 0 auto;
  max-width: 768px;
  padding: 5rem 1.75rem 1.75rem;
}
.heroe h1 {
  margin: 0;
  font-size: 3rem;
  font-weight: 600;
  text-align: center;
  font-family: "Noto Serif JP";
  letter-spacing: -0.02em;
  color: #fff;
}

.heroe h2 {
  font-size: 1rem;
  font-weight: 600;
  text-align: center;
  font-family: "Noto Serif JP";
  letter-spacing: -0.02em;
  color: #fff;
}

.heroe h2 + p {
  margin: -0.5rem 0 0;
}

.heroe p {
  font-size: 1.25rem;
  font-weight: 300;
  text-align: center;
  color: #d3dceb;
}

.heroe ul {
  display: flex;
  justify-content: center;
  list-style: none;
  padding: 0 1em 0 0;
}
.heroe ul > li {
  margin: 1rem 1rem 0;
}
.heroe ul > li::before {
  display: inline-block;
  content: '▶︎';
  color: #fff;
  margin: 0 0.5em 0.325em 0;
  font-size: 0.75em;
  vertical-align: middle;
}

.copyrights {
  padding: 0.25rem 1.75rem;
  text-align: center;
  color: #fff;
  font-family: "Noto Serif JP";
  font-weight: 400;
}
</style>
</head>
<body>

{% block content %}{% endblock %}

</body>
</html>